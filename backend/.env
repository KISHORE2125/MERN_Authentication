# Server configuration
PORT=3001
NODE_ENV=development

# MongoDB connection string
# Replace <username>, <password>, <cluster-url>, <dbname> with your values
MONGOURI=mongodb+srv://kishore:Az123456@cluster0.lz141ya.mongodb.net/

# JWT secret key for authentication
JWT_SECRET=your_super_secret_key_here

# Optional: token expiration (if you want to customize)
JWT_EXPIRES_IN=1h
